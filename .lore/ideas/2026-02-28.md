# 2026-02-28

- There is a seperation of concerns problems that is making me lose sight of building an agentic native application. The Claude Agent SDK interaction is kind of spread through out and I'd prefer it isoloated into one location, otherwise changes in how we use it needs to be duplicated. Then we have the behavior from the web app which mixes the API and the frontend, and the daemon API. Should the web api and the web frontend be seperated? Then should the toolboxes actually be apis? If everything was an API then giving it to an agent or the user is easier, ain't it?
