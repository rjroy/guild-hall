# 2026-02-23
- The prompts for workers should be additional system prompts, not replacements; we still want the full power of the Claude Code system prompt.
- Need some basic workers defined: researcher which only knows the lore files, systems designer which has full access to the entire project but only works on lore files, and implementer which has full access to the entire project and can work on all files. The researcher is there to do outside world researcher agnostic of the system and is used to provide context for the other two.
- Given most of the packages are going to be in the git directory how does the `~/.guild-hall/packages/` directory actually work in practice? Is there an initial setup step which copies the packages from the git directory to the guild hall directory? Or do we need to have some sort of symlink system in place (which suggests being have more than 1 deep directories)? Or do we just have the workers read directly from the git directory and ignore the guild hall directory entirely? Combination?
- Phase 7 hardening: Post-merge sync needs a trigger beyond daemon restart. After the user squash-merges a PR on GitHub, `claude/main` needs to sync with master. Currently this only happens at daemon startup or via `guild-hall sync` CLI. The manager should be able to trigger this (sync_project tool or API endpoint) so the user can say "I merged the PR" in a meeting and the manager handles it without restarting.
